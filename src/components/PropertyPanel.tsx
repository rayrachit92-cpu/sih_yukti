import type { Property3D } from '../types/property';
import { GlassPanel } from './GlassPanel';
export function PropertyPanel({property}:{property?:Property3D}){if(!property)return <GlassPanel className="p-5"><p className="text-slate-400 text-sm">Select a parcel to inspect its 3D property record.</p></GlassPanel>; return <GlassPanel className="p-5 space-y-3"><div className="flex justify-between"><b>{property.name}</b><span className="text-xs text-cyan-300">{property.status}</span></div>{[['3D Height',`${property.buildingHeight.toFixed(2)} m`],['Floors',property.floors],['DEM',property.dem?.toFixed(2)],['DSM',property.dsm?.toFixed(2)],['nDSM',property.ndsm?.toFixed(2)],['Confidence',`${property.confidence}%`]].map(([a,b])=><div className="flex justify-between border-b border-white/5 py-2 text-sm" key={String(a)}><span className="text-slate-500">{a}</span><span>{b}</span></div>)}</GlassPanel>}
